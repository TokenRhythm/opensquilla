{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://opensquilla.dev/contracts/gateway/v4/workspaces/workspaces-git-commit.schema.json",
  "title": "OpenSquilla workspaces.git.commit Contract",
  "description": "Commit the index of a trusted project workspace with the supplied message. Only the index is committed: nothing is staged implicitly, because a commit that silently includes files the operator did not choose is the mistake this surface exists to prevent. Git's own identity and signing configuration stay in force.",
  "type": "object",
  "additionalProperties": false,
  "properties": {"request": {"$ref": "#/$defs/WorkspacesGitCommitRequestFrame"}, "response": {"$ref": "#/$defs/WorkspacesGitCommitResponseFrame"}},
  "x-opensquilla-wire": {"protocol": "opensquilla-websocket-json", "version": 4, "compatibility": "exact-json-tree"},
  "x-opensquilla-codegen": {"python": {"tool": "datamodel-code-generator", "version": "0.81.0", "target": "pydantic_v2.BaseModel"}, "typescript": {"tool": "json-schema-to-typescript", "version": "16.0.0"}, "runtimeValidation": {"tool": "ajv", "version": "8.20.0", "mode": "standalone-adapter-only"}},
  "x-opensquilla-method": {"name": "workspaces.git.commit", "kind": "command", "scope": "operator.write", "guestAllowed": false, "idempotency": "non-idempotent", "timeout": {"policy": "transport"}, "capability": {"kind": "method-availability", "name": "workspaces.git.commit"}, "request": "#/$defs/WorkspacesGitCommitRequestFrame", "params": "#/$defs/WorkspacesGitCommitParams", "response": "#/$defs/WorkspacesGitCommitResponseFrame", "result": "#/$defs/WorkspacesGitCommitResult", "errors": [{"code": "OWNER_REQUIRED"}, {"code": "WORKSPACE_NOT_FOUND"}, {"code": "INVALID_PARAMS"}, {"code": "NOTHING_STAGED"}, {"code": "GIT_FAILED"}, {"code": "UNAVAILABLE"}, {"code": "INTERNAL_ERROR"}]},
  "$defs": {
    "WorkspacesGitCommitParams": {"type": "object", "additionalProperties": false, "required": ["workspaceId", "message"], "properties": {"workspaceId": {"type": "string", "minLength": 1}, "message": {"type": "string", "minLength": 1, "maxLength": 4096, "description": "The commit message. Its first line becomes the reported subject."}}},
    "WorkspacesGitCommitResult": {"type": "object", "additionalProperties": false, "required": ["sha", "subject"], "properties": {"sha": {"type": "string", "description": "The created commit, or an empty string if Git could not report it."}, "subject": {"type": "string", "minLength": 1, "description": "The first line of the message that was committed."}}},
    "RpcError": {"type": "object", "additionalProperties": true},
    "WorkspacesGitCommitRequestFrame": {"type": "object", "additionalProperties": true, "required": ["type", "id", "method", "params"], "properties": {"type": {"const": "req"}, "id": {"type": "string"}, "method": {"const": "workspaces.git.commit"}, "params": {"$ref": "#/$defs/WorkspacesGitCommitParams"}}},
    "WorkspacesGitCommitResponseFrame": {"type": "object", "additionalProperties": true, "required": ["type", "id", "ok"], "properties": {"type": {"const": "res"}, "id": {"type": "string"}, "ok": {"type": "boolean"}, "payload": {"$ref": "#/$defs/WorkspacesGitCommitResult"}, "error": {"$ref": "#/$defs/RpcError"}}}
  }
}
