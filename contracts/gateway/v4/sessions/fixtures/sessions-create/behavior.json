{
  "format_version": 1,
  "kind": "behavior",
  "cases": [
    { "id": "behavior.default-agent", "coverage": "agentId defaults to main", "expectation": "exact" },
    { "id": "behavior.kind-alias", "coverage": "kind web maps to webchat namespace", "expectation": "exact" },
    { "id": "behavior.workspace-owner", "coverage": "workspaceId requires local owner", "expectation": "exact" },
    { "id": "behavior.seed-message", "coverage": "message is appended after creation", "expectation": "exact" },
    { "id": "behavior.no-manager", "coverage": "bare create returns compatibility stub", "expectation": "exact" }
  ]
}
