# 2026-08-22 子代理路由与思维强度实验

## 已完成的改动

### 1. c3 fusion 修复
- `[llm_ensemble].enabled` → true（5 候选并行提案）
- 之前 `_SelectorFallbackProvider` 出现≠异常，是常规包装器名

### 2. 子代理 bypass 路由（代码已实现，未提交）
- `squilla_router.py:1903-1941`：检测 `:subagent:` → 跳分类器 → 直接钉固定 tier
- 优先级：显式 spawn max_tier > `[agents_defaults.subagents].max_tier` > 硬编码 c1
- bypass 会传递 thinking_level 元数据（L1933）
- 分支：pr/custom-provider，ahead 5

### 3. 子代理默认天花板（已配置 + 热加载）
```toml
[agents_defaults.subagents]
max_tier = "c0"
```

### 4. 思维强度对照实验
同模型(ox-alpha)、同任务(数学证明)、同输入(17613 tokens)，只变 thinking_level：

| 档位 | thinking | 输出 tokens | 延迟 |
|---|---|---|---|
| c0 | `""` | 2,780 | 93.7s |
| c1 | `"high"` | 2,494 | 88.1s |
| c2 | `"xhigh"` | 3,261 | 108.6s |

- **thinking 真实生效**，但 c0 空字符串≠"不思考"，而是"模型自行决定"
- 建议 c0 改显式 `"low"`

### 5. 热重载方法
- 匿名调用被拒；需从 config [auth] 段提取 token → `OPENSQUILLA_GATEWAY_TOKEN` 环境变量 → 再调 `opensquilla gateway reload`

## 当前 config 快照
```toml
default_tier = "c2"
c0: ox-alpha, thinking=""
c1: ox-alpha, thinking="high"
c2: ox-alpha, thinking="xhigh"
c3: deepseek-v4-flash, ensemble_enabled=true, thinking="xhigh"
[llm_ensemble] enabled=true
[agents_defaults.subagents] max_tier="c0"
```

## 待办
1. c0 thinking → `"low"`（一行配置）
2. 可观测性：同模型档位 model 字段失去区分度（decision log 有 thinking_level 可交叉区分）
3. 路由效率：c1/c2 同模型时分类器区分两档的信息增益
4. c3 作为决策档分配子代理（主人后续讨论）
5. 成本反弹控制：等数据量够再讨论 max_tool_calls 预算层